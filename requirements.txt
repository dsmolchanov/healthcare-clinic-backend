# FastAPI and Server
fastapi>=0.115.14  # Allow updates for starlette CVE fixes
uvicorn[standard]==0.32.0
python-dotenv==1.0.0
python-multipart==0.0.22  # Fixed: CVE-2024-24762, CVE-2024-53981, CVE-2026-24486
starlette>=0.49.1  # Explicit pin for CVE fixes

# Database
supabase==2.15.0
gotrue==2.12.4  # Upgraded for httpx 0.28+ compatibility
asyncpg==0.29.0

# HTTP Client
httpx==0.28.1
requests==2.32.4  # CVE-2024-47081

# Pydantic
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.1.1  # Required for pydantic EmailStr validation

# Security-critical packages - keep >=  for automatic security updates
cryptography>=43.0.0
certifi>=2024.0.0
PyJWT==2.10.1  # JWT authentication (Phase 5)
# Note: protobuf CVE-2026-0994 affects ALL versions, no fix released yet

# Redis Cache
redis==5.0.1
hiredis==2.2.3  # C parser for better Redis performance
zstandard>=0.22.0  # Compression for Redis cache - flexible version

# Observability
prometheus-client==0.19.0
langfuse>=3.10.0

# LLM Providers (native clients - not LangChain wrappers)
openai==1.58.1
google-genai==1.40.0

# LangGraph for orchestration
langchain-core>=0.3.81  # CVE-2025-65106, CVE-2025-68664
langgraph>=0.2.76  # Latest 0.2.x before 1.0 breaking changes

# Document Processing
beautifulsoup4==4.12.2
pymupdf==1.23.8
python-docx==1.1.0
pytesseract==0.3.10
Pillow>=11.0.0  # Keep >= for security updates
markdown==3.5.1
tiktoken==0.8.0

# Google Calendar Integration
google-api-python-client==2.187.0
google-auth==2.45.0
google-auth-httplib2==0.3.0

# Microsoft Graph SDK for Outlook Calendar
msgraph-sdk>=1.50.0
azure-identity>=1.19.0

# Utilities
psutil==5.9.6
apscheduler==3.10.4
boto3==1.34.0
python-dateutil==2.9.0
jsonschema==4.23.0
PyYAML==6.0.1
cachetools==5.5.0

# Language Processing
langdetect==1.0.9
rapidfuzz==3.10.1
jinja2==3.1.6  # CVE-2024-56326, CVE-2024-56201, CVE-2025-27516
babel==2.16.0
phonenumbers==8.13.52  # Phone number validation for WhatsApp pre-population

# Stripe Payment Integration
stripe==11.3.0

# OpenTelemetry for Arize Cloud (upgraded for langfuse>=3.10 compatibility)
opentelemetry-api>=1.33.1
opentelemetry-sdk>=1.33.1
opentelemetry-exporter-otlp-proto-http>=1.33.1
openinference-instrumentation-google-genai>=0.1.1
openinference-instrumentation-langchain>=0.1.35

# ============================================
# REMOVED DEPENDENCIES (Phase 6 cleanup):
# ============================================
# langchain-openai - Using native OpenAI client instead
# langchain-google-genai - Using native google-genai client instead
# langchain-community - Not used (ChatZhipuAI not imported)
# zhipuai - GLM adapter uses OpenAI-compatible API
# python-jose - Replaced by PyJWT
# passlib - Not imported anywhere
# lxml - Not imported anywhere
# chardet - Not imported anywhere
# python-magic - Not imported anywhere
# trafilatura - Not imported anywhere
# extruct - Not imported anywhere
# ============================================
